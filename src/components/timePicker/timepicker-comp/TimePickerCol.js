import React,{Component} from 'react'

class TimePickerCol extends Component {
    constructor(props) {
        super(props)
        this.state = {
            defaultValue: this.props.defaultValue ? this.props.defaultValue : null,
        }
    }

    render() {
        <div>
            
        </div>
    }
}

export default TimePickerCol